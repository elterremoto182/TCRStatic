[build]
  publish = "out"
  command = "npm run build:optimized"

[[plugins]]
  package = "netlify-plugin-inline-critical-css"

  [plugins.inputs]
  # Only process key pages to avoid timeout (skip 100+ blog posts)
  directoryFilter = ["!blog"]

